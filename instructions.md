# 📘 Roadmap обучения: Умный таск-трекер с интеллектуальным распределением задач

---

## Этап 0 — Настройка GitHub и CI/CD

1. **Создать репозиторий на GitHub**

    * публичный или приватный — по желанию.
    * клонировать локально.

2. **Подготовить структуру для CI**

    * папка `.github/workflows/`.
    * файл `ci.yml` (или аналог в GitLab CI).

3. **Логика CI/CD (без кода, пояснения):**

    * **Триггер:** push в `main` / pull request.
    * **Шаги:**

        1. Checkout репозитория.
        2. Установка окружения (PHP, Composer, Node, Go).
        3. Установка зависимостей.
        4. Поднятие тестовой БД (Postgres), Redis, RabbitMQ (контейнеры).
        5. Запуск миграций/seeders на тестовой базе.
        6. Запуск unit/integration тестов (PHP + Go).
        7. (Опция) Сборка Docker-образов.
        8. (Опция) Деплой на тестовый стенд.

4. **Зачем CI/CD:**

    * автоматизация тестов и сборки,
    * единый процесс для команды,
    * быстрые обратные проверки при PR.

---

## Цель проекта

Создать рабочий таск-трекер, где задачи автоматически распределяются между исполнителями по навыкам, загрузке и приоритетам. Проект прокачивает: архитектуру, микросервисы, очереди, шардирование/кэш, тестирование, CI/CD, frontend и DevOps.

---

## Стек (рекомендация)

* **Backend (монолит):** PHP 8+ (Laravel)
* **Heavy / Microservices:** Go
* **DB:** PostgreSQL
* **Кэш / очередь:** Redis / RabbitMQ
* **Frontend:** React + TypeScript
* **Мониторинг:** Prometheus + Grafana
* **Документация API:** Swagger / OpenAPI
* **Real-time:** WebSocket (Broadcasting)
* **Контейнеризация:** Docker + docker-compose
* **CI/CD:** GitHub Actions (или GitLab CI)

---

## Общие принципы архитектуры

* **Микросервисный подход + монолит:** рабочий монолит на PHP (UI, CRUD, auth), отдельные Go-сервисы для heavy-compute и background.
* **Чистая архитектура:** transport слои (контроллеры) ≠ бизнес-слой (сервисы) ≠ инфраструктура (репозитории).
* **События и очереди:** domain events вместо синхронных вызовов для асинхронной интеграции.
* **Паттерны:** Repository, Service Layer, Observer/Event, Decorator (опционально).
* **Идемпотентность:** обработчики событий должны быть идемпотентны.
* **Мониторинг и логирование:** метрики + трассировка для наблюдаемости.

---

## Этапы реализации (пошагово)

### Этап 1 — Подготовка окружения

* Настроить `docker-compose` с контейнерами: `php-fpm`/nginx, Postgres, Redis, RabbitMQ, prometheus, grafana, optional: elasticsearch.
* Инициализировать Laravel-проект и проверить подключение к Postgres.
* Подключить базовую метрику/экспортер для Prometheus.
* **Зачем:** воспроизводимое окружение, удобство локальной разработки и тестирования.

---

### Этап 2 — Проектирование БД

* Основные сущности: `users`, `projects`, `tasks`, `task_comments`, `task_logs`, `skills` (опционально).
* Поля:

    * `tasks`: title, description, status, priority, assignee_id, reporter_id, project_id, meta(JSONB).
    * `users`: name, email, roles, skills(JSONB), capacity/availability.
* Связи: `User hasMany Tasks`, `Project hasMany Tasks`.
* Seeders / фиктивные данные для тестов.
* **Зачем:** заранее продуманная модель облегчает масштабирование и интеграцию с индексами/поиском.

---

### Этап 3 — CRUD и REST API

* Создать контроллеры для Projects, Tasks, Users.
* Валидация через FormRequest или эквивалент.
* Resource-классы для API-ответов (стандартизированные JSON).
* Swagger/OpenAPI для автогенерации документации.
* Кэширование на Redis (cache-aside для read heavy endpoint'ов).
* **Зачем:** стабильный API для фронтенда и внешних сервисов; кэш ускоряет чтение.

---

### Этап 4 — Авторизация и RBAC

* JWT для API (или session для web).
* Роли: Admin, Manager, User.
* Policies/Guards для доступа к операциям (create, edit, assign, delete).
* **Зачем:** безопасность, разделение прав и контекстов.

---

### Этап 5 — Сервисный слой и архитектурные паттерны

* Вынести логику в сервисы: `TaskService`, `UserService`, `ProjectService`.
* Репозитории: абстрагировать ORM (Eloquent) для тестов и инверсии зависимостей.
* Events/Observers: при создании/обновлении задач публиковать domain events.
* **Зачем:** поддерживаемость, тестируемость, уменьшение зависимости контроллеров от логики.

---

### Этап 6 — Интеллектуальное распределение задач (Go-сервис)

* Описание:

    * Go-сервис берет задачу, анализирует доступных пользователей по навыкам, загруженности и приоритету, и предлагает/назначает лучшего кандидата.
    * Сервис подписан на очередь с событиями `task.created`, `task.reassignment.request`.
* Интеграция:

    * Laravel ставит job/публикует событие в очередь.
    * Go worker читает событие, выполняет расчёт и публикует `task.assigned` или результат в API Laravel.
* Алгоритмы:

    * scoring по навыкам/приоритету/заряду (weighting).
    * учёт availability и SLA.
* **Зачем:** heavy compute вынесен в Go; масштабируемость и производительность.

---

### Этап 7 — Очереди и асинхронность

* Выбрать Redis (simple) или RabbitMQ/Kafka (production).
* Паттерты:

    * producer: Laravel публикует события;
    * consumer: Go воркеры обрабатывают задачи;
    * DLQ (dead letter) для неуспешных сообщений;
    * retry/backoff для устойчивости.
* **Зачем:** отделить синхронные запросы от тяжёлых вычислений, повысить надёжность.

---

### Этап 8 — Frontend (React + TypeScript)

* Мини SPA: проекты → список задач → карточка задачи → фильтры.
* Реализация real-time: WebSocket (или Laravel Broadcasting через Redis) для мгновенных уведомлений об изменениях/назначениях.
* UX: drag & drop для kanban (opti).
* **Зачем:** интерактивный интерфейс, удобная визуализация нагрузки и назначений.

---

### Этап 9 — Тестирование

* **Unit tests:** сервисы, репозитории в Laravel; алгоритмы распределения в Go.
* **Integration tests:** end-to-end сценарии с поднятым тестовым стеком (DB, queue).
* **Mocks / Fakes:** заглушки для Go-сервиса при тестировании PHP и наоборот.
* **Зачем:** надёжность, возможность менять код с уверенностью.

---

### Этап 10 — Docker + CI/CD + Деплой

* Dockerize: Dockerfile для PHP app, Go-сервисов; общая `docker-compose.yml`.
* CI: настроить pipeline (тесты, линты, сборка образов).
* Деплой: staging/production (docker registry → k8s или VPS/compose).
* Настроить Prometheus экспортеры и Grafana дашборды (latency, queue length, worker errors).
* **Зачем:** автоматизация, наблюдаемость и стабильность.

---

## Особые моменты / рекомендации

* **Idempotency:** все consumers должны безопасно обрабатывать повторные сообщения.
* **Schema evolution:** при изменении структуры событий использовать версионирование сообщений или backward-compatible поля.
* **Observability:** логи с trace_id, метрики (task processing time, queue lag), трассировка запросов между сервисами.
* **Backpressure:** механизм, чтобы воркеры не перегружались (rate limits, autoscaling).
* **Security:** валидация входных данных, JWT, проверка прав при назначениях.
* **Data contracts:** описывать события в OpenAPI/AsyncAPI для согласованности между сервисами.

---

## Как учиться с AI (практика)

1. **Пошаговые объяснения:** на каждом этапе AI объясняет, зачем это делаем и какие trade-offs.
2. **Сравнение опций:** AI даст плюсы/минусы (RabbitMQ vs Kafka, Redis vs Memcached и т.д.).
3. **Интерактивность:** задавай вопросы по конкретным участкам архитектуры — AI разберёт плюсы/минусы и даст варианты реализаций.
4. **Проверка архитектуры:** AI может проверить твой план или PR по критериям production readiness.

---

## Итог (что прокачаешь)

* проект даёт практику: PHP/Laravel, Go, Postgres, Redis/RabbitMQ, React/TypeScript, Docker, CI/CD, мониторинг.
* разберёшь event-driven архитектуру, idempotency, eventual consistency, дизайн событий, background processing.
* получишь репозиторий в GitHub с CI pipeline и демонстрацией архитектурных паттернов.

---
